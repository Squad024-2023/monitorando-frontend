import { sunSeed } from "@/app/fonts";
import styles from "./nav.module.css";
export default function Nav() {
  return (
    <nav className={styles.nav}>
    <div  className={sunSeed.className}>monitorando</div>
    </nav>
  )
}
